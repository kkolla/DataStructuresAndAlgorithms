WE see how the data is stored inside the computer's memory.
We use this platform to play with colors to stdout.

We use this place to make operation with respect to memory. like converting a number to OCT, HEX, Bin is supported.
